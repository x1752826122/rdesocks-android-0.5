name: Build darkk/redsocks for Android (arm64)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-arm64:
    runs-on: ubuntu-latest
    env:
      API_LEVEL: 23

    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Clone darkk/redsocks
        run: |
          git clone --depth=1 https://github.com/darkk/redsocks.git

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 确认并获取预装的NDK路径
        run: |
          echo "当前环境已安装的NDK版本："
          ls -la /usr/local/lib/android/sdk/ndk/
          
          export NDK_VERSION=$(ls /usr/local/lib/android/sdk/ndk/ | head -n 1)
          export NDK_PATH="/usr/local/lib/android/sdk/ndk/$NDK_VERSION"
          
          if [ ! -d "$NDK_PATH" ]; then
            echo "错误：未找到NDK目录"
            exit 1
          fi
          
          echo "使用的NDK路径：$NDK_PATH"
          echo "NDK_PATH=$NDK_PATH" >> $GITHUB_ENV

      - name: Prepare toolchain variables
        run: |
          TOOLCHAIN="${{ env.NDK_PATH }}/toolchains/llvm/prebuilt/linux-x86_64"
          echo "TOOLCHAIN=$TOOLCHAIN" >> $GITHUB_ENV
        shell: bash

      - name: Build libevent for aarch64
        run: |
          set -euo pipefail
          TOOLCHAIN="${{ env.TOOLCHAIN }}"
          export CC="$TOOLCHAIN/bin/aarch64-linux-android${{ env.API_LEVEL }}-clang"
          export AR="$TOOLCHAIN/bin/llvm-ar"
          export RANLIB="$TOOLCHAIN/bin/llvm-ranlib"
          export STRIP="$TOOLCHAIN/bin/llvm-strip"
          export PATH="$TOOLCHAIN/bin:$PATH"

          git clone --depth=1 https://github.com/libevent/libevent.git
          cd libevent
          ./autogen.sh || true
          ./configure --host=aarch64-linux-android --enable-shared=no --enable-static=yes --prefix=$(pwd)/../libevent-prefix CC="$CC" CFLAGS="--sysroot=$TOOLCHAIN/sysroot -fPIC" AR="$AR" RANLIB="$RANLIB" STRIP="$STRIP"
          make -j$(nproc)
          make install
          cd ..

      - name: Build redsocks (aarch64) linking libevent
        run: |
          set -euo pipefail
          TOOLCHAIN="${{ env.TOOLCHAIN }}"
          export CC="$TOOLCHAIN/bin/aarch64-linux-android${{ env.API_LEVEL }}-clang"
          export AR="$TOOLCHAIN/bin/llvm-ar"
          export RANLIB="$TOOLCHAIN/bin/llvm-ranlib"
          export SYSROOT="$TOOLCHAIN/sysroot"
          export LDFLAGS_SYSROOT="-L$SYSROOT/usr/lib/aarch64-linux-android/${{ env.API_LEVEL }}"
          export PATH="$TOOLCHAIN/bin:$PATH"

          cd redsocks
          export GIT_DIR=$(pwd)/.git || true
          export PREFIX="$(pwd)/../libevent-prefix"
          export CPPFLAGS="-I${PREFIX}/include"
          export LDFLAGS="$LDFLAGS_SYSROOT -L${PREFIX}/lib"
          # 移除-lpthread（Android中无需单独链接）
          export LIBS="-levent -ldl -lm"

          chmod +x configure || true
          make clean || true
          make CC="$CC" CPPFLAGS="$CPPFLAGS" LDFLAGS="$LDFLAGS" LIBS="$LIBS" LD="$CC" CFLAGS="-Wno-error" -j$(nproc)

          mkdir -p ../out/arm64-v8a
          if [ -f redsocks ]; then
            cp redsocks ../out/arm64-v8a/redsocks
            file ../out/arm64-v8a/redsocks
          else
            echo "Error: redsocks binary not found"
            exit 1
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: redsocks-android-arm64
          path: out/**
